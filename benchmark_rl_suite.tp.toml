commands = [
    "rm -f rl_training_smoke_output.txt rl_kernel_benchmark_output.txt rl_kernel_benchmark_results.json rl_kernel_benchmark_results.md rl_training_canary_safe_output.txt rl_training_results_canary_safe.json rl_training_results_canary_safe.md rl_training_full_experimental_output.txt rl_training_results_full_experimental.json rl_training_results_full_experimental.md full-rl-results.md",
    "python3 -m venv --system-site-packages .venv",
    "source .venv/bin/activate && pip install --upgrade pip -q",
    "source .venv/bin/activate && pip install -q pytest ruff fastapi 'openai>=1.30,<2' 'httpx>=0.27,<1' 'numpy>=1.26,<2' 'transformers>=4.40,<5'",
    "source .venv/bin/activate && pip install -e . --no-deps -q",
    "source .venv/bin/activate && python - <<'PY'\nimport torch\nimport triton\nprint('torch', torch.__version__)\nprint('cuda', torch.version.cuda)\nprint('cuda_available', torch.cuda.is_available())\nprint('device', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'none')\nprint('triton', triton.__version__)\nPY",
    "bash -lc 'set -o pipefail; source .venv/bin/activate && python benchmark_rl_training.py --suite canary --patch-mode safe --repeats 1 --search-rounds 1 --candidates-per-round 1 --top-k 1 2>&1 | tee rl_training_smoke_output.txt'",
    "bash -lc 'set -o pipefail; source .venv/bin/activate && python benchmark_rl_kernels.py 2>&1 | tee rl_kernel_benchmark_output.txt'",
    "bash -lc 'set -o pipefail; source .venv/bin/activate && python benchmark_rl_training.py --suite canary --patch-mode safe 2>&1 | tee rl_training_canary_safe_output.txt'",
    "bash -lc 'set -o pipefail; source .venv/bin/activate && python benchmark_rl_training.py --suite full --patch-mode experimental 2>&1 | tee rl_training_full_experimental_output.txt'",
]

outputs = [
    "rl_training_smoke_output.txt",
    "rl_kernel_benchmark_output.txt",
    "rl_kernel_benchmark_results.json",
    "rl_kernel_benchmark_results.md",
    "rl_training_canary_safe_output.txt",
    "rl_training_results_canary_safe.json",
    "rl_training_results_canary_safe.md",
    "rl_training_full_experimental_output.txt",
    "rl_training_results_full_experimental.json",
    "rl_training_results_full_experimental.md",
]

ignore = [
    ".venv",
    "__pycache__/",
    ".git",
    ".pytest_cache",
    "*.egg-info",
    "runs/",
]
