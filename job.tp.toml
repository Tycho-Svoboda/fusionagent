commands = [
    "apt-get update -qq && apt-get install -y -qq python3.12-venv > /dev/null 2>&1",
    "python3 -m venv .venv",
    "source .venv/bin/activate && pip install --upgrade pip -q && pip install -e '.[dev]' -q",
    "source .venv/bin/activate && export $(cat .env | xargs) && nvidia-smi --query-gpu=name,memory.total --format=csv,noheader && python benchmark_e2e.py 2>&1 | tee benchmark_output.txt",
]

outputs = [
    "benchmark_output.txt",
]

ignore = [
    ".venv",
    "__pycache__/",
    ".git",
    ".pytest_cache",
    "*.egg-info",
    "runs/",
]
