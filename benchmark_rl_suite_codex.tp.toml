commands = [
    "rm -f rl_training_smoke_output.txt rl_kernel_benchmark_output.txt rl_kernel_benchmark_results.json rl_kernel_benchmark_results.md rl_training_canary_safe_output.txt rl_training_results_canary_safe.json rl_training_results_canary_safe.md rl_training_full_experimental_output.txt rl_training_results_full_experimental.json rl_training_results_full_experimental.md full-rl-results.md",
    "rm -rf .venv .codex_home .tp_env.sh",
    "python3 -m venv --system-site-packages .venv",
    "bash -lc 'echo \"=== Bootstrap: Python env ===\" && source .venv/bin/activate && pip install --upgrade pip -q && pip install -q pytest ruff fastapi \"httpx>=0.27,<1\" \"numpy>=1.26,<2\" \"transformers>=4.40,<5\"'",
    "bash -lc 'echo \"=== Bootstrap: Node + Codex CLI ===\" && curl -fsSL https://fnm.vercel.app/install | bash -s -- --skip-shell && export PATH=\"$HOME/.local/share/fnm:$PATH\" && eval \"$($HOME/.local/share/fnm/fnm env --shell bash)\" && fnm install 22 && npm install -g @openai/codex@0.130.0'",
    "bash -lc 'echo \"=== Bootstrap: Codex auth ===\" && mkdir -p .codex_home && cp .codex_job_auth/auth.json .codex_home/ && cp .codex_job_auth/config.toml .codex_home/ && cp .codex_job_auth/version.json .codex_home/ && chmod 600 .codex_home/auth.json .codex_home/config.toml && printf \"%s\\n\" \"export PATH=\\\"$HOME/.local/share/fnm:$PATH\\\"\" \"eval \\\"$($HOME/.local/share/fnm/fnm env --shell bash)\\\"\" \"source \\\"$PWD/.venv/bin/activate\\\"\" \"export CODEX_HOME=\\\"$PWD/.codex_home\\\"\" \"export FUSIONAGENT_LLM_BACKEND=\\\"codex_cli\\\"\" \"export PYTHONUNBUFFERED=1\" > .tp_env.sh'",
    "bash -lc 'source .tp_env.sh && pip install -e . --no-deps -q'",
    "bash -lc 'echo \"=== Bootstrap: Versions ===\" && source .tp_env.sh && python -c \"import os, torch, triton; print(\\\"torch\\\", torch.__version__); print(\\\"cuda\\\", torch.version.cuda); print(\\\"cuda_available\\\", torch.cuda.is_available()); print(\\\"device\\\", torch.cuda.get_device_name(0) if torch.cuda.is_available() else \\\"none\\\"); print(\\\"triton\\\", triton.__version__); print(\\\"llm_backend\\\", os.environ.get(\\\"FUSIONAGENT_LLM_BACKEND\\\"))\" && codex --version && codex login status'",
    "bash -lc 'set -euo pipefail; source .tp_env.sh; echo \"=== Stage 1: smoke ===\"; python -u benchmark_rl_training.py --suite canary --patch-mode safe --repeats 1 --search-rounds 1 --candidates-per-round 1 --top-k 1 2>&1 | tee rl_training_smoke_output.txt'",
    "bash -lc 'set -euo pipefail; source .tp_env.sh; echo \"=== Stage 2: kernel benchmark ===\"; python -u benchmark_rl_kernels.py 2>&1 | tee rl_kernel_benchmark_output.txt'",
    "bash -lc 'set -euo pipefail; source .tp_env.sh; echo \"=== Stage 3: canary safe ===\"; python -u benchmark_rl_training.py --suite canary --patch-mode safe 2>&1 | tee rl_training_canary_safe_output.txt'",
    "bash -lc 'set -euo pipefail; source .tp_env.sh; echo \"=== Stage 4: full experimental ===\"; python -u benchmark_rl_training.py --suite full --patch-mode experimental 2>&1 | tee rl_training_full_experimental_output.txt'",
]

outputs = [
    "rl_training_smoke_output.txt",
    "rl_kernel_benchmark_output.txt",
    "rl_kernel_benchmark_results.json",
    "rl_kernel_benchmark_results.md",
    "rl_training_canary_safe_output.txt",
    "rl_training_results_canary_safe.json",
    "rl_training_results_canary_safe.md",
    "rl_training_full_experimental_output.txt",
    "rl_training_results_full_experimental.json",
    "rl_training_results_full_experimental.md",
]

ignore = [
    ".venv",
    ".codex_home",
    "__pycache__/",
    ".git",
    ".pytest_cache",
    "*.egg-info",
    "runs/",
]
